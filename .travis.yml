language: node_js
node_js: stable

before_install:
  - npm install -g npm
  - npm install -g yarn

install:
  - yarn install

script:
  - hexo clean
  - hexo g

after_script:
  - hexo deploy
# E: Build LifeCycle

env:
 global:
   - GH_REF: github.com:warriorsworld/warriorsworld.github.io.git